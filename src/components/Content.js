// import ItemList from "./ItemList";
import Table from "./Table";
const Content = ({items}) => {
    return (
        // <ItemList items = {items} />
        <Table items= {items} />
    )
}
export default Content;